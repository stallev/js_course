# Руководство по заданиям TypeScript (для авторов)

- Только `.ts` в sandbox. Нет React/`next_sandbox`.
- Источник — практические блоки в `typescript/topics/`.
- `node browser/gen-pages.mjs` из `js_sandbox` пересобирает HTML.
- Не коммитить `topics/**/*.js` (gitignore).

Каркас темы:

```
topics/topic_N/
├── exercises.ts
├── solutions.ts
└── browser/index.html
```
