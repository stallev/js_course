/**
 * Тема 3 — Примитивы и вывод типов
 */
export function describePrimitives(): number {
  // TODO: залогируй строку, число, boolean; верни длину строки
  return 0;
}

if (typeof document === "undefined") {
  console.log(describePrimitives());
}
